{
  "name": "ipscscore",
  "version": "2.0.0",
  "private": true,
  "description": "IPSC/USPSA scoring application",
  "workspaces": [
    "backend",
    "frontend",
    "electron"
  ],
  "scripts": {
    "dev:docker": "docker compose up",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:electron": "cd electron && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:electron": "cd electron && npm run build",
    "build:mac": "npm run build:backend && npm run build:frontend && cd electron && npm run build:mac",
    "build:win": "npm run build:backend && npm run build:frontend && cd electron && npm run build:win",
    "build:linux": "npm run build:backend && npm run build:frontend && cd electron && npm run build:linux",
    "build:linux-arm64": "npm run build:backend && npm run build:frontend && cd electron && npm run build:linux-arm64",
    "build:all": "npm run build:backend && npm run build:frontend && cd electron && npm run build:all",
    "version:patch": "node scripts/version-bump.mjs patch",
    "version:minor": "node scripts/version-bump.mjs minor",
    "version:major": "node scripts/version-bump.mjs major"
  }
}
